import React from "react";
import { fetchRandomPokemon } from "../util";

const Home = () => {

    // console.log(fetchRandomPokemon());
    // fetchRandomPokemon()
    // const resultName = result.name;
    // const name = resultName[0].toUpperCase() + resultName.slice(1);
    // console.log(name);
    
    return <>
    <h1> Sup Home</h1>
    </>
};

export default Home;